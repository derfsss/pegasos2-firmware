# Build artifacts
build/*.o
build/*.elf
build/*.bin
build/*.map
build/*.lst
build/*.d

# Spec-author handoff note (preserved locally; not part of the
# firmware project's public repo content)
HANDOFF.md

# Editor / OS cruft
*.swp
*~
.DS_Store
Thumbs.db

# QEMU test output
test/out/
test/*.log

# Local AOS4 test images (copied from E:\Emulators\... per
# CONTRIBUTING.md test plan). Never bundled.
hd1.raw
hd0.qcow2
hd2.raw
