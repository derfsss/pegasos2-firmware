# Build artifacts
build/*.o
build/*.elf
build/*.bin
build/*.map
build/*.lst
build/*.d

# Spec-author handoff note (preserved locally; not part of the
# firmware project's public repo content)
HANDOFF.md

# Editor / OS cruft
*.swp
*~
.DS_Store
Thumbs.db

# QEMU test output
test/out/
test/*.log
